{{- if .Values.bucketweb.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "thanos.fullname" . }}-bucket-web
{{- if .Values.bucketweb.httpService.annotations }}
  annotations:
{{ toYaml .Values.bucketweb.httpService.annotations | indent 4 }}
{{- end }}
  labels:
    app.kubernetes.io/name: {{ include "thanos.name" . }}
    helm.sh/chart: {{ include "thanos.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/component: bucket-web
spec:
  type: {{ .Values.bucketweb.httpService.type }}
  {{- if .Values.bucketweb.httpService.externalTrafficPolicy }}
  externalTrafficPolicy: {{ .Values.bucketweb.httpService.externalTrafficPolicy }}
  {{- end }}
  ports:
    - port: 10902
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "thanos.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: bucket-web
{{- end -}}
